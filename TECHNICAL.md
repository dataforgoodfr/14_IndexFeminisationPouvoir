# Configurations pour l'exécution

1. Commande `playwright install` pour installer les binaires Playwright.
2. Commande `playwright install-deps` pour installer (sur Ubuntu) les paquets nécessaires à l'exécution de Playwright.
3. 